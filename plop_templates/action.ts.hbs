import { {{pascalCase name}}Model } from '../models/{{kebabCase name}}.model'

export namespace {{pascalCase name}} {
    export class AddOrUpdate {
        static readonly type = '[{{pascalCase name}}] AddOrUpdate';
        constructor(public payload: {{pascalCase name}}Model) {}
    }
    export class Clear {
    static readonly type = '[{{pascalCase name}}] Clear';
  }

  export class RemoveByIds {
    static readonly type = '[{{pascalCase name}}] RemoveById';
    constructor(public payload: string[]) {}
  }
}