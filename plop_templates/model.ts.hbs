export type {{pascalCase name}}Model = {
    id: string
}

export type {{pascalCase name}}StateModel = {map: Record<string, {{pascalCase name}}Model>};

export const defaults: {{pascalCase name}}StateModel = {
    map: {},
};